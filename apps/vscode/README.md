# DebugScope for VS Code

This package contributes a compact live waveform view to the VS Code Bottom Panel.
Opening the view attaches to an existing local DebugScope Hub or starts one on the
configured UDP and HTTP ports. Use **Open in Browser** for the full workbench.
